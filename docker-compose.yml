services:
  opd:
    build: .
    ports:
      - "${OPD_PORT:-8000}:8000"
    volumes:
      - opd_data:/data
    restart: unless-stopped
    networks:
      - default
      - proxy

volumes:
  opd_data:

networks:
  proxy:
    external: true
    name: ${PROXY_NETWORK:-proxy}
